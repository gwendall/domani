{
  "name": "domani-cli",
  "version": "0.4.31",
  "type": "module",
  "description": "Domain infrastructure for AI agents. Search, register, DNS, email, connect - all from your terminal. Installs the `domani` command.",
  "license": "MIT",
  "author": "domani.run",
  "homepage": "https://domani.run",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gwendall/domani.git"
  },
  "bin": {
    "domani": "dist/domani.cjs"
  },
  "files": [
    "dist/domani.cjs",
    "LICENSE",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "bump": "node -e \"const fs=require('fs');const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const old=pkg.version;const p=old.split('.').map(Number);pkg.version=[p[0],p[1],p[2]+1].join('.');fs.writeFileSync('package.json',JSON.stringify(pkg,null,2)+'\\n');const version=JSON.parse(fs.readFileSync('version.json','utf8'));version.version=pkg.version;fs.writeFileSync('version.json',JSON.stringify(version,null,2)+'\\n');console.log('CLI version '+old+' → '+pkg.version)\"",
    "build": "node build.mjs",
    "install:local": "node build.mjs && cp dist/domani.cjs $(which domani)",
    "check": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "npx tsx --test src/__tests__/*.test.ts",
    "test:e2e": "npx tsx --test src/__tests__/e2e/*.test.ts",
    "test:e2e:full": "E2E_MUTATE=1 npx tsx --test src/__tests__/e2e/*.test.ts",
    "test:all": "npx tsx --test src/__tests__/*.test.ts src/__tests__/e2e/*.test.ts",
    "dev": "tsc --watch",
    "publish:npm": "node build.mjs && npm publish --access public"
  },
  "keywords": [
    "domain",
    "domains",
    "dns",
    "registrar",
    "cli",
    "email",
    "mcp",
    "ai-agent",
    "whois",
    "nameserver",
    "transfer",
    "ssl",
    "parking",
    "webhooks",
    "x402",
    "usdc"
  ],
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "commander": "^13.1.0",
    "picocolors": "^1.1.1",
    "yocto-spinner": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.3",
    "tsx": "^4.23.1",
    "typescript": "^5.0.0"
  }
}
